#include <cmath>
#include <iostream>
#include <vector>

void print_vector_info(std::vector<double> &vec)
{

    std::cout << "Size: " << vec.size() << std::endl;
    std::cout << "Capacity: " << vec.capacity() << std::endl;
    std::cout << "Empty?: " << std::boolalpha << vec.empty() << std::endl;
}

int main()
{

    const return 0;
}
